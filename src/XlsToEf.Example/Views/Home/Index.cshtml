@{
    ViewBag.Title = "Home Page";
}

<div class="jumbotron">
  <p>
    <a href="#" data-url="@Url.Action("ImportModal", "Home")" class="btn btn-default" data-toggle="modal" data-target="#modal">Import</a>

  </p>
</div>
